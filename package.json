{
  "name": "ai-website",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/user-event": "^13.5.0",
    "firebase": "^11.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "deploy": "xcopy src public /E /H /C /I /EXCLUDE:exclude.txt && firebase deploy --only hosting:littlelegendsbooks"
  },
  "devDependencies": {
    "webpack-dev-server": "^5.1.0"
  }
}
